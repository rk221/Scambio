- if item_genre_game.enable?
    = link_to t('link_to.enable'), admin_disable_item_genre_game_path(item_genre_game), method: :post, remote: true, class: 'btn btn-success'
- else  
    = link_to t('link_to.disable'), admin_enable_item_genre_game_path(item_genre_game), method: :post, remote: true, class: 'btn btn-danger'
